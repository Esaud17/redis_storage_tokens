source "https://rubygems.org"

# Specify your gem's dependencies in redis_storage_tokens.gemspec
gemspec

gem "rake", "~> 12.0"
gem "rspec", "~> 3.0"

# Use Redis adapter to run Action Cable in production
gem 'redis', '~> 4.0'
# Use Redis adapter to run namespace in production
gem 'redis-namespace', '~> 1.5', '>= 1.5.2'
# Use cache storage redis on rails